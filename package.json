{
  "name": "@lichess/api",
  "version": "0.1.4",
  "keywords": [
    "api",
    "lichess"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/lichess-api/typescript.git"
  },
  "license": "MIT",
  "author": "gameroman",
  "type": "module",
  "imports": {
    "#lib/*": "./src/lib/*.ts",
    "#schemas": "./src/schemas/index.ts"
  },
  "exports": {
    ".": "./dist/index.mjs",
    "./schemas": "./dist/schemas.mjs",
    "./package.json": "./package.json"
  },
  "types": "./dist/index.d.mts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "bunx --bun tsdown",
    "format": "biome check --write",
    "gen": "bun run --sequential gen:all format",
    "gen:all": "bun run --parallel gen:schemas gen:client",
    "gen:client": "bun ./scripts/gen-client",
    "gen:schemas": "bun ./scripts/gen-schemas",
    "lint": "biome check",
    "prepublishOnly": "bun run build",
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "minizod": "0.0.3"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.7",
    "@gameroman/config": "0.2.8",
    "@types/bun": "1.3.14",
    "tsdown": "0.22.14",
    "typescript": "7.0.2",
    "zod": "4.4.3"
  },
  "engines": {
    "node": ">=24"
  }
}
